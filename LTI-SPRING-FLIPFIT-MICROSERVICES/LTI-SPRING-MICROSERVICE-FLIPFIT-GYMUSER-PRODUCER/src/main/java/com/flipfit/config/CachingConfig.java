package com.flipfit.config;

//@Configuration
public class CachingConfig {
	
	/*
	 * @Bean public CacheManager cacheManager() {
	 * 
	 * // ConcurrentMapCacheManager cacheManager = new
	 * ConcurrentMapCacheManager("myCache"); // ConcurrentMapCacheManager
	 * cacheManager1 = new ConcurrentMapCacheManager("roleCache");
	 * 
	 * SimpleCacheManager cacheManager = new SimpleCacheManager();
	 * cacheManager.setCaches(Arrays.asList( new ConcurrentMapCache("myCache"), new
	 * ConcurrentMapCache("roleCache") )); return cacheManager;
	 * 
	 * 
	 * }
	 */

}
