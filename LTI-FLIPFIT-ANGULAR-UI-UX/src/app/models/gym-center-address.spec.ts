import { GymCenterAddress } from './gym-center-address';

describe('GymCenterAddress', () => {
  it('should create an instance', () => {
    expect(new GymCenterAddress()).toBeTruthy();
  });
});
